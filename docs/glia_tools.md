# Glia Extended Command Roadmap

This document outlines 200 additional commands for the `glia` tool to further automate and optimize developer workflows.

## Git & Version Control
2. `glia git-cleanup-local`: Deletes all local branches that have been merged into the main branch.*
6. `glia git-find-commit <pattern>`: Searches commit messages for a specific pattern.*

## Build & Dependency Management
31. `glia build-lint-cmake`: Checks CMakeLists.txt for best practices and errors.

How does it know what unit tests to run?
## Testing & Quality Assurance
41. `glia test-unit`: Runs all unit tests.

65. `glia env-shell <service>`: Opens an interactive shell in a running container.
70. `glia env-set-var <key> <value>`: Sets an environment variable in the local config.
71. `glia env-list-vars`: Lists all configured environment variables.
79. `glia env-clean-volumes`: Removes unused Docker volumes.
80. `glia env-doctor`: Diagnoses common environment issues and suggests fixes.

## Documentation & Knowledge

84. `glia doc-spellcheck`: Runs a spellchecker across all Markdown files.
85. `glia doc-todo-list`: Extracts all TODO/FIXME comments from the codebase into a report.
86. `glia doc-create-readme`: Generates a standardized README.md based on project metadata.

90. `glia doc-update-changelog`: Automatically updates CHANGELOG.md based on git history.

## Code Quality & Linting
104. `glia lint-check-format`: Verifies if the code is properly formatted without changing it.
108. `glia lint-comments`: Checks for missing or outdated comments and docstrings.
109. `glia lint-naming`: Verifies that variable and function names follow project conventions.
110. `glia lint-security`: Performs a static security analysis (SAST) on the code.
111. `glia lint-secrets`: Scans for accidentally committed secrets or API keys.
112. `glia lint-perf-hints`: Provides static analysis hints for performance improvements.
113. `glia lint-deprecated`: Identifies usage of deprecated APIs or functions.
114. `glia lint-headers`: Verifies that all files have the required license headers.
115. `glia lint-markdown`: Lints Markdown files for consistency and style.
116. `glia lint-yaml`: Validates all YAML configuration files.
117. `glia lint-json`: Validates all JSON files.
118. `glia lint-dockerfile`: Lints the project's Dockerfiles.

139. `glia prof-binary-diff`: Compares binary size and symbols between two builds.
152. `glia code-review-submit`: Submits a code review with comments.
160. `glia feedback-collect`: Runs a CLI survey for internal tool feedback.

## Project Management
162. `glia proj-info`: Displays project metadata, versions, and key metrics.
163. `glia proj-health`: calculates a project health score based on various metrics.
164. `glia proj-tech-stack`: Identifies and lists all technologies used in the project.
165. `glia proj-estimate`: Provides a rough estimate of project size (LOC, files).

181. `glia sys-info`: Displays information about the local development machine.
182. `glia sys-clean-temp`: Cleans system-level temporary files generated by glia.
183. `glia sys-update-glia`: Checks for and installs updates for the glia tool itself.
184. `glia sys-alias-setup`: Sets up shell aliases for common glia commands.
185. `glia sys-bash-completions`: Generates and installs bash completion scripts.
187. `glia sys-check-updates`: Checks for updates to core system development tools.
191. `glia sys-disk-usage`: Analyzes disk usage of development directories.
195. `glia sys-clipboard-copy`: Copies the output of the last command to the clipboard.
197. `glia sys-search <query>`: A fast local search across all development projects.
200. `glia sys-help-interactive`: Launches an interactive walkthrough of all glia features.
