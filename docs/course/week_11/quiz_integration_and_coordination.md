# Quiz: Integration, Coordination, and Biological Logic

**Total Points:** 10
**Instructions:** Select the most accurate answer for each question.

---

## 1. What are the "Three Sacred Methods" of a SDD Card?
A) Init, Run, Stop
B) Setup, Execute, Report
C) Seed, Root, Harvest
D) Input, Process, Output

## 2. In a multi-agent swarm, what prevents two agents from conflicting on the same module?
A) A Fact Lock
B) A Slack notification
C) A GitHub Pull Request
D) A 3D Light-Curtain

## 3. What is "High-Fidelity Reporting"?
A) Returning a simple `true` or `false`.
B) Providing dense, evidence-based data like complexity scores and line counts.
C) Running tests with 100% code coverage.
D) Using a high-resolution monitor.

## 4. What is a "Reasoning Guard"?
A) A physical security guard in the greenhouse.
B) A Green Syntax operator that evaluates the *justification* for a code change.
C) A firewall that blocks external APIs.
D) A tool that auto-fixes syntax errors.

## 5. What happens when a "health_index" fact drops below 0.5 in a self-healing trellis?
A) The agent is fired.
B) The system automatically triggers a `PRUNE` command.
C) The greenhouse lights turn red.
D) Nothing, it's just a warning.

---

## Answer Key
1: B, 2: A, 3: B, 4: B, 5: B
